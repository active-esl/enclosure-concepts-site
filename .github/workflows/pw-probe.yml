name: PW probe
on: workflow_dispatch
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - run: echo ok-pages-write && date -u
