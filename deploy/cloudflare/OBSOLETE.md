# Obsolete — Cloudflare Worker host (retired 2026-08-06)

The public gallery is **GitHub Pages** only:

- **Share:** https://concepts.active-esl.com/
- **Repo:** `active-esl/enclosure-concepts-site`

Do **not** run `deploy.sh` or `wrangler deploy`. Workers for
`enclosure-concepts-site` were removed; `*.workers.dev` URLs are not the share
path.

These files remain as historical reference only.
